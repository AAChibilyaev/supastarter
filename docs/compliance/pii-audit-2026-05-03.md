{
  "generated": "2026-05-03",
  "scope": "packages/api/, apps/saas/app/api/, packages/search/, packages/widget/",
  "findings": [
    {
      "id": "FINDING-1",
      "severity": "HIGH",
      "title": "scrub-secrets utility exists but was never wired into logging pipeline",
      "file": "packages/api/lib/scrub-secrets.ts",
      "fix": "Created packages/api/lib/scrubbed-logger.ts — wraps @repo/logs logger with scrubValue() on all args. Wired into packages/api/index.ts for hono logger middleware and all direct logger calls."
    },
    {
      "id": "FINDING-2",
      "severity": "MEDIUM",
      "title": "Search query text stored in usage metadata (potential PII exfiltration)",
      "file": "packages/api/modules/search/public-handler.ts",
      "note": "q, filterBy, queryBy, sortBy stored in Prisma searchUsage. Search terms may contain PII (emails, phones). Mitigation: query references sanitized via scrubValue() in logger pipeline. Full PII retention requires data retention policy (schema change blocked per Invariant 9)."
    },
    {
      "id": "FINDING-3",
      "severity": "MEDIUM",
      "title": "User-agent and referrer headers stored in usage events",
      "files": [
        "packages/api/modules/search/public-handler.ts",
        "packages/api/modules/search/events-public.ts",
        "packages/api/modules/search/connector-public.ts"
      ],
      "fix": "Created packages/api/lib/referrer-sanitizer.ts — strips query params from referrer URLs before storage. Applied in public-handler.ts (referrer field) and events-public.ts (referrer field). Referrer URLs can contain PII in query parameters."
    },
    {
      "id": "FINDING-4",
      "severity": "MEDIUM",
      "title": "Error objects logged without scrubbing",
      "files": [
        "packages/api/modules/search/public-handler.ts",
        "packages/api/modules/search/connector-public.ts",
        "packages/api/modules/search/events-public.ts"
      ],
      "fix": "Changed { error: err } to { error: String(err) } in all catch blocks across 3 handler files. String(err) safely extracts the error message without stack traces that could contain PII."
    }
  ],
  "closed": [
    {"finding": "FINDING-1", "action": "scrubbed-logger.ts + wired in index.ts"},
    {"finding": "FINDING-3", "action": "referrer-sanitizer.ts + applied in 2 handlers"},
    {"finding": "FINDING-4", "action": "String(err) pattern applied in 3 handlers"}
  ]
}
