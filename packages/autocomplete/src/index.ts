/**
 * @repo/autocomplete — AACSearch autocomplete source adapter
 *
 * Re-exports the {@link createAACsearchSource} function and its option type.
 *
 * @example
 * ```typescript
 * import { createAACsearchSource } from '@aacsearch/autocomplete';
 * ```
 */

export { createAACsearchSource } from "./createSource";
export type { CreateAACsearchSourceOptions } from "./createSource";
